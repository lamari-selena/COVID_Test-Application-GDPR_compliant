server:
  port: 8888

spring:
 cloud:
  config:
   server:
    git:
     uri: https://github.com/AyoubMoujane/covid-alert-backend-config.git
     username: ${GITHUB_USERNAME}
     password: ${GITHUB_TOKEN}
     default-label: "main"